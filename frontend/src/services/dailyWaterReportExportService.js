function escapeHtml(value) { return String(value ?? '').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;'); }
function fmt(value) { if(value===null||value===undefined||value==='')return '—'; const n=Number(value); return Number.isFinite(n)?n.toLocaleString('es-MX',{minimumFractionDigits:2,maximumFractionDigits:2}):String(value); }
export function buildDailyWaterReportHtml(report) { const section=(title, rows)=>`<h2>${title}</h2><table><thead><tr><th>Elemento</th><th>Flujo actual</th><th>Apertura</th><th>Cierre</th><th>Volumen</th><th>Actividad</th><th>Comunicación</th><th>Última actualización</th></tr></thead><tbody>${rows.map(row=>`<tr><td>${escapeHtml(row.name)}</td><td>${row.flow==null?'—':`${fmt(row.flow)} ${escapeHtml(row.flow_unit||'L/s')}`}</td><td>${row.opening_m3==null?'—':`${fmt(row.opening_m3)} m³`}</td><td>${row.closing_m3==null?'—':`${fmt(row.closing_m3)} m³`}</td><td>${row.volume_m3==null?escapeHtml(row.activity):`${fmt(row.volume_m3)} m³`}</td><td>${escapeHtml(row.activity)}</td><td>${escapeHtml(row.communication)}</td><td>${escapeHtml(row.last_update||'Sin lectura')}</td></tr>`).join('')}</tbody></table>`; return `<!doctype html><html lang="es"><head><meta charset="utf-8"><title>${escapeHtml(report.title)}</title><style>body{font-family:Arial;color:#152536;margin:30px}h1{text-align:center}h2{color:#075985;margin-top:28px}table{width:100%;border-collapse:collapse;font-size:12px}th{background:#0b6e8e;color:#fff}th,td{border:1px solid #b6cbd6;padding:7px;text-align:left}tbody tr:nth-child(even){background:#f3f8fa}.summary{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.summary div{border:1px solid #cbdde5;padding:10px}</style></head><body><h1>Reporte Diario de Control Hídrico Durango</h1><p>Periodo: ${escapeHtml(report.period_label)}</p><div class="summary"><div>Pozos<br><strong>${fmt(report.summary?.well_volume_m3)} m³</strong></div><div>Líneas<br><strong>${fmt(report.summary?.line_volume_m3)} m³</strong></div><div>Flujos auxiliares<br><strong>${fmt(report.summary?.flow_volume_m3)} m³</strong></div><div>Total operativo<br><strong>${fmt(report.summary?.total_operational_m3)} m³</strong></div></div>${section('Pozos',report.wells?.rows||[])}${section('Líneas',report.production_lines?.rows||[])}${section('Flujos auxiliares',report.operational_flows?.rows||[])}</body></html>`; }
export function exportDailyWaterReportHtml(report) { const blob=new Blob([buildDailyWaterReportHtml(report)],{type:'text/html;charset=utf-8'}); const url=URL.createObjectURL(blob); const a=document.createElement('a'); a.href=url; a.download=`reporte-diario-control-hidrico-durango-${report.start_date}.html`; document.body.appendChild(a); a.click(); a.remove(); URL.revokeObjectURL(url); }
